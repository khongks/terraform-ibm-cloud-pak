apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ${sub_name}
  namespace: ${namespace}
spec:
  channel: ${channel_version}
  name: ${sub_name}
  source: ${source}
  sourceNamespace: ${source_namespace}